# HARD RULES

- **Bắt buộc load thư mục `.agents` khi vào session**: Khi bắt đầu bất kỳ session làm việc nào, AI/Agent bắt buộc phải load toàn bộ nội dung và các quy tắc nằm trong thư mục [`.agents`](file:///d:/fui-lms/.agents) (đặc biệt là thư mục [`rules`](file:///d:/fui-lms/.agents/rules)) để đảm bảo tuân thủ đúng các chỉ dẫn và quy trình tư duy của dự án.

**cấm viết script execute db**